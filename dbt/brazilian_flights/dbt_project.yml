name: 'brazilian_flights'
version: '1.0.0'
profile: 'brazilian_flights'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  brazilian_flights:
    bronze:
      +schema: bronze
      +catalog: workspace
    silver:
      +schema: silver
      +catalog: workspace
    gold:
      +schema: gold
      +catalog: workspace
